{
  "version": 0,
  "description": "NDC gate policy. Inside protectedPaths every file must be a .ndc container. Source code and config stay plaintext so builds keep working.",
  "protectedPaths": [
    "data/**",
    "datasets/**",
    "secrets/**"
  ],
  "allowPlaintext": [
    "**/README.md",
    "**/.gitkeep",
    "**/.gitignore"
  ],
  "notes": "Enforced by magic bytes (NDLB) — must be a REAL sealed container, not just a .ndc name. A renamed plaintext file is rejected. Governed-open is a later iteration."
}
