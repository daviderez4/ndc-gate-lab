# OPTIONAL transparent mode (iteration 1). Routes protected paths through the
# ndc clean/smudge filter so files keep normal names while git stores sealed
# bytes. Enable per-clone (cannot be forced from a clone — by design):
#   git config filter.ndc.clean  "node tools/ndc-filter.mjs clean %f"
#   git config filter.ndc.smudge "node tools/ndc-filter.mjs smudge %f"
#   git config filter.ndc.required true
#
# Left commented so the lab's default flow stays the explicit .ndc model.
# Uncomment to try smooth mode.
#
# data/**     filter=ndc -text
# datasets/** filter=ndc -text
# secrets/**  filter=ndc -text
